# Local replacement for the one thing run_model.py/run_calibration.py actually used from the
# `mnch` package (cat-mnch/mnch/constants.py) - avoids needing that package importable/installed.

analysis_window = (2020, 2050)  # Year range for result analysis
